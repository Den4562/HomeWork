<Window x:Class="WpаFindWord.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpаFindWord"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Слово для поиска в файле:" Margin="10" />
        <TextBox x:Name="WordTextBox" Grid.Row="0" Margin="164,10,10,10" />

        <TextBlock Text="Путь к файлу:" Grid.Row="1" Margin="11,10,-11,50" Grid.RowSpan="2" />
        <TextBox x:Name="FilePathTextBox" Grid.Row="1" Margin="117,10,10,10" />

        <Button Content="Поиск" Grid.Row="2" Width="100" Height="30" Margin="10" Click="SearchButton_Click" />

        <Label Content="" x:Name="ResultLabel" Grid.Row="3" Margin="10" />
    </Grid>
</Window>
